py-cord
python-dotenv==1.0.1
motor==3.3.2
PyNaCl==1.5.0 # Or the latest stable version
# beanie==1.24.0   <-- REMOVE THIS LINE
# aiohttp==3.9.5   <-- REMOVE THIS LINE (if it was there)
# msgspec==0.17.0  <-- REMOVE THIS LINE (it's a beanie dependency, so no longer needed)
